spring.application.name=spring_boot

# 数据库连接配置 - 腾讯云云数据库
spring.datasource.url=jdbc:mysql://gz-cdb-73hxuxal.sql.tencentcdb.com:26655/text_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&connectTimeout=60000&socketTimeout=60000&autoReconnect=true&failOverReadOnly=false&maxReconnects=10
spring.datasource.username=root
spring.datasource.password=lht13213
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# 数据库连接池配置 - HikariCP (Spring Boot 默认连接池)
# 连接超时时间 (毫秒) - 设置为60秒
spring.datasource.hikari.connection-timeout=60000
# 连接最大生命周期 (毫秒) - 设置为30分钟
spring.datasource.hikari.max-lifetime=1800000
# 连接空闲超时时间 (毫秒) - 设置为10分钟
spring.datasource.hikari.idle-timeout=600000
# 最大连接池大小
spring.datasource.hikari.maximum-pool-size=20
# 最小空闲连接数
spring.datasource.hikari.minimum-idle=5
# 连接测试查询
spring.datasource.hikari.connection-test-query=SELECT 1

# JPA配置
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Hibernate 查询超时配置
spring.jpa.properties.hibernate.jdbc.timeout=300
spring.jpa.properties.hibernate.statement.inspector=org.hibernate.resource.jdbc.spi.StatementInspector

# 服务器配置
server.port=8080
